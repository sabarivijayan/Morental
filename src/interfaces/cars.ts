export interface CarCardProps {
    title: string;
    category: string;
    imageUrl: string;
    fuelCapacity: string;
    transmission: string;
    capacity: string;
    price: string;
    discountedPrice?: string; // Optional property
}
